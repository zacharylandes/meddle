      <nav class="navbar navbar-expand-lg   col-md-12" id="main-nav">
            <div class = 'logo-div'>
                <%= link_to image_tag('https://s3.amazonaws.com/pytdating/pytlogo.png'), homes_path %>
            </div>
            <h4>Meddle</h4>
            <a class="navbar-brand" id='header-title'></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
             <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                    <%if current_user%>
                        <%=link_to 'Your Dashboard', dashboard_path(current_user)%>
                        <%=link_to 'Search all daters', users_path%>
                    <%end%>
                    </li>
                    <a class='media' href='/conversations'>
                        <i class="far fa-envelope"></i>
                    </a>
                </ul>
            </div>
            <div class = 'login' id='login'>
                <% if current_user %>
                   Signed in as <%= current_user.f_name %>
                    <%= link_to "Sign out", signout_path %>
                <% else %>
                    <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in", class:'' %>
                 <% end %>
            </div>
      </nav>