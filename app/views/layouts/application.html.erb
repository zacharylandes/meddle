<!DOCTYPE html>
<html lang="zxx" class="no-js">
  <head>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>PYT</title>
  <%= favicon_link_tag('favicon-32x32.png')%>
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <div class = 'layer'>
  </div>
    <body class='col-md-12'>
      <nav class="navbar navbar-expand-lg navbar-light bg-light col-md-12" id="main-nav">
        <div class = 'logo-div'>
          <%= link_to image_tag('pytlogo'), homes_path %>
          </div>
        <a class="navbar-brand" id='header-title'>PYT Dating</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item active">
            <%if current_user%>
            <%=link_to 'Your Dashboard', dashboard_path(current_user), class: 'nav-link'%>
            <%end%>
            </li>
          </ul>
        </div>
        <div class = 'login'>
          <% if current_user %>
          Signed in as <strong><%= current_user.f_name %></strong>!
          <%= link_to "Sign out", signout_path, id: "sign_out", class:'' %>
          <% else %>
          <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in", class:'' %>
          <% end %>
        </div>
      </nav>
      <div class = 'main col-md-12' >
       <% if flash[:notice] %>
         <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <%= yield %>
      </div>
      <footer class= 'col-md-12' id="footer ">
      </footer>
    </body>
</html>
